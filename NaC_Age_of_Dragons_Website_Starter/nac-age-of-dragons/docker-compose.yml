services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_DB: nac
      POSTGRES_USER: nac
      POSTGRES_PASSWORD: change-me
    ports:
      - "5432:5432"
    volumes:
      - nac-postgres:/var/lib/postgresql/data

volumes:
  nac-postgres:
